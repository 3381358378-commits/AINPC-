/**
 * ============================================================================
 *  首页 (Home Page) —— 项目保留但当前未实际使用
 * ============================================================================
 *
 *  【关于此页面】
 *  根据项目硬约束：小程序启动时直接跳转到地图探索页 (pages/map-explore)，
 *  不经过此首页。此页面作为早期模板保留，功能是展示"拓拓 AI 助手"欢迎语，
 *  提供两个入口按钮：
 *    1. [进入对话]  → 跳转到 chat 页（自由闲聊模式）
 *    2. [地图探索]  → 跳转到 map-explore 页（沉浸式游览模式）
 *
 *  ⚠️  目前 app.json 的 pages 数组第一项是 map-explore（首屏），此页面排在
 *      第二位，用户只能通过 wx.navigateBack + wx.reLaunch 的兜底路径
 *      访问到这里（见 map-explore.js 的 onToolHome 方法）。
 *
 *  【未来扩展】
 *  若后续需要增加"游客模式 / 剧情选择 / 设置页"等前置流程，
 *  可以把 app.json 的 pages 第一项改回 index，恢复此页面作为真正的首屏。
 * ============================================================================
 */

Page({
  data: {},

  /**
   * 跳转按钮：直接进入聊天页（自由闲聊模式，不绑定任何景点）
   */
  goToChat() {
    wx.navigateTo({
      url: '/pages/chat/chat'
    })
  },

  /**
   * 跳转按钮：进入地图探索页（沉浸式游览模式）
   */
  goToMap() {
    wx.navigateTo({
      url: '/pages/map-explore/map-explore'
    })
  }
})
